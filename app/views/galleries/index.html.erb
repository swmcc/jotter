<div class="min-h-screen bg-gray-50 flex flex-col">
  <!-- Header -->
  <header class="bg-white border-b border-gray-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
      <div class="flex items-center justify-between">
        <div class="flex items-center gap-3">
          <%= link_to root_path, class: "hover:opacity-70" do %>
            <h1 class="text-4xl">📓</h1>
          <% end %>
          <% unless authenticated? %>
            <p class="text-sm text-gray-600">Public galleries</p>
          <% end %>
        </div>
        <div class="flex items-center gap-4">
          <% if authenticated? %>
            <span class="text-sm text-gray-700"><%= Current.session.user.email_address %></span>
            <%= button_to "Sign out", session_path, method: :delete, class: "text-sm text-gray-600 hover:text-gray-900 underline" %>
          <% else %>
            <%= link_to "Sign in", new_session_path, class: "text-sm text-blue-600 hover:text-blue-900 underline" %>
          <% end %>
        </div>
      </div>
    </div>
  </header>

  <!-- Main Content -->
  <main class="flex-1 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <!-- Page Header -->
    <div class="mb-6 flex items-center justify-between">
      <div>
        <h2 class="text-3xl font-bold text-gray-900">Galleries</h2>
        <p class="text-gray-600 mt-1"><%= @galleries.count %> galleries</p>
      </div>
      <% if authenticated? %>
        <%= link_to "New Gallery", new_gallery_path, class: "rounded bg-blue-600 hover:bg-blue-700 px-4 py-2 text-white font-medium transition-colors" %>
      <% end %>
    </div>

    <!-- Search and Filter -->
    <div class="mb-6 bg-white rounded-lg shadow-sm border border-gray-200 p-4">
      <%= form_with url: galleries_path, method: :get, class: "flex gap-4" do |f| %>
        <div class="flex-1">
          <%= f.text_field :q, placeholder: "Search galleries...", value: params[:q], class: "block w-full rounded border border-gray-300 px-3 py-2 text-gray-900 placeholder-gray-400 focus:border-blue-500 focus:ring-1 focus:ring-blue-500 focus:outline-none" %>
        </div>
        <%= f.submit "Search", class: "rounded bg-gray-600 hover:bg-gray-700 px-4 py-2 text-white font-medium cursor-pointer transition-colors" %>
        <% if params[:q].present? || params[:tag].present? %>
          <%= link_to "Clear", galleries_path, class: "rounded bg-gray-200 hover:bg-gray-300 px-4 py-2 text-gray-700 font-medium transition-colors" %>
        <% end %>
      <% end %>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-4 gap-6">
      <!-- Sidebar - Tags -->
      <div class="lg:col-span-1">
        <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-4">
          <h3 class="font-semibold text-gray-900 mb-3">Tags</h3>
          <div class="flex flex-wrap gap-2">
            <% @tags.each do |tag| %>
              <%= link_to galleries_path(tag: tag.name), class: "inline-block px-3 py-1 text-sm rounded-full #{params[:tag] == tag.name ? 'bg-blue-600 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200'} transition-colors" do %>
                <%= tag.name %>
              <% end %>
            <% end %>
          </div>
        </div>
      </div>

      <!-- Galleries Grid -->
      <div class="lg:col-span-3">
        <% if @galleries.any? %>
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
            <% @galleries.each do |gallery| %>
              <%= link_to gallery_path(gallery), class: "group" do %>
                <div class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden hover:shadow-md transition-shadow">
                  <!-- Cover Image -->
                  <div class="aspect-square bg-gray-200 relative overflow-hidden">
                    <% if gallery.cover_photo_or_default&.image&.attached? %>
                      <%= image_tag gallery.cover_photo_or_default.thumbnail, class: "w-full h-full object-cover" %>
                    <% else %>
                      <div class="w-full h-full flex items-center justify-center text-gray-400 text-6xl">
                        🖼️
                      </div>
                    <% end %>
                  </div>

                  <!-- Gallery Info -->
                  <div class="p-4">
                    <h3 class="text-lg font-semibold text-gray-900 group-hover:text-blue-600 transition-colors truncate">
                      <%= gallery.title %>
                    </h3>
                    <% if gallery.description.present? %>
                      <p class="text-gray-600 text-sm mt-1 line-clamp-2"><%= gallery.description %></p>
                    <% end %>

                    <div class="flex items-center gap-2 mt-2 text-xs text-gray-500">
                      <span><%= gallery.is_public ? "🌐 Public" : "🔒 Private" %></span>
                      <span>•</span>
                      <span><%= gallery.albums.count %> albums</span>
                    </div>

                    <% if gallery.tags.any? %>
                      <div class="flex flex-wrap gap-1 mt-2">
                        <% gallery.tags.first(3).each do |tag| %>
                          <span class="inline-block px-2 py-0.5 text-xs rounded-full bg-gray-100 text-gray-700">
                            <%= tag.name %>
                          </span>
                        <% end %>
                      </div>
                    <% end %>
                  </div>
                </div>
              <% end %>
            <% end %>
          </div>
        <% else %>
          <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-8 text-center">
            <p class="text-gray-600">No galleries found</p>
            <% if authenticated? %>
              <%= link_to "Create your first gallery", new_gallery_path, class: "text-blue-600 hover:underline mt-2 inline-block" %>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  </main>

  <%= render "shared/footer" %>
</div>
